version: '3.8'

services:
  webapp:
    build: .
    container_name: webapp-container
    ports:
      - "8080:9090"              # Host 8080 → Container 9090
    environment:
      - JAVA_OPTS=-Xmx512m       # Example Java options